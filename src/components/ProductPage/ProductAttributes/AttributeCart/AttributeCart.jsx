import React from 'react';
import classes from './AttributeCart.module.css'

const AttributeCart = ({currentAttributes}) => {
    console.log(currentAttributes)
    return (
        <div className={classes.attributeCart}>
        </div>
    );
};

export default AttributeCart;