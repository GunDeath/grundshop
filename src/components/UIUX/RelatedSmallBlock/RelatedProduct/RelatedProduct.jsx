import React from 'react';
import classes from './RelatedProduct.module.css'

const RelatedProduct = ({product}) => {
    return (
        <div className={classes.blockRelatedProduct}>
        </div>
    );
};

export default RelatedProduct;